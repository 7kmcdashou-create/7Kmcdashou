#!/bin/bash
# One-liner to run in Codespace terminal:
# curl -fsSL https://get.docker.com | sudo bash && sudo usermod -aG docker vscode && newgrp docker && cd /workspaces/7Kmcdashou && sudo docker compose up -d && sleep 10 && npm install -g hermes-web-ui && nohup hermes-web-ui start 8648 --no-open &
